# Copy this file to .env.secrets and fill in real values.
# .env.secrets is never committed to version control.
#
# Docker Compose uses this file in two ways:
#   1. env_file — injects these vars into each container's environment
#   2. Variable substitution in docker-compose.yml (${POSTGRES_PASSWORD})
#      requires the vars to be available on the host. Easiest way:
#
#        ln -s .env.secrets .env
#
# Generate a secure JWT secret:
#   openssl rand -base64 64

# ── Shared ────────────────────────────────────────────────────────────────────

# JWT signing secret (auth-service, url-service, analytics-service)
JWT_SECRET=change_me

# ── Database ──────────────────────────────────────────────────────────────────

# PostgreSQL password — used by the db container and all backend services
# (referenced as ${POSTGRES_PASSWORD} in DATABASE_URL values in docker-compose.yml)
POSTGRES_PASSWORD=change_me

# ── Notification ──────────────────────────────────────────────────────────────

# SMTP password for notification-service
EMAIL_PASS=change_me
