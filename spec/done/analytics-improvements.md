# Analytics Service Improvements

> **Status:** done
> **Service(s):** analytics-service
> **Priority:** high

## Goal

The analytics-service currently stores raw events but offers no way to query them
meaningfully. It has no authentication, no pagination, no aggregation, and no data
lifecycle management. This spec makes it a real analytics service rather than a
write-only event log.

## Background

The service exposes a single `GET /events` endpoint that returns every row in the
database with no filtering or pagination. As data grows this will degrade performance
and eventually become unusable. The endpoint is also completely unauthenticated, leaking
user emails and URL data to any caller. There are no indexes on the Event table.

## Requirements

### Functional

- **R1:** `GET /events` shall require JWT authentication. Admin users see all events;
  regular users see only events related to their own user ID.
- **R2:** `GET /events` shall support pagination via `page` and `limit` query parameters,
  defaulting to page 1, limit 50. The response shall include total count.
- **R3:** `GET /events` shall support filtering by `type`, `from` (ISO date), `to` (ISO
  date), and `userId`.
- **R4:** The service shall expose `GET /analytics/urls/:shortId/clicks` returning total
  click count and today's click count for the given URL.
- **R5:** The service shall expose `GET /analytics/summary` (admin only) returning total
  user count, total URL count, and total click count across all time.
- **R6:** The service shall expose `GET /analytics/top-urls` (admin only) returning the
  top N URLs by click count for a given time period (`period` query param, e.g. `7d`,
  `30d`; `limit` query param, default 10).
- **R7:** The service shall run a daily cleanup job that deletes events older than a
  configurable retention period (default 90 days, controlled by `EVENT_RETENTION_DAYS`
  env var).
- **R8:** Event payloads shall be validated against per-type schemas before being
  persisted. Invalid payloads shall be rejected with a log warning, not stored silently.

### Non-Functional

- **R9:** The `Event` table shall have indexes on `type`, `createdAt`, and the composite
  `(type, createdAt)` to support efficient filtering queries.
- **R10:** Aggregation queries shall not load raw rows into memory; use database-level
  aggregation (`COUNT`, `GROUP BY`).

## Acceptance Criteria

- [x] `GET /events` without a token returns 401.
- [x] `GET /events` with a regular user token returns only events where the payload
      `userId` matches the authenticated user.
- [x] `GET /events` with an admin token returns all events.
- [x] `GET /events?page=2&limit=10` returns the correct page and includes
      `{ pagination: { page, limit, total } }` in the response.
- [x] `GET /events?type=url_clicked` returns only events of that type.
- [x] `GET /events?from=2026-01-01&to=2026-02-01` returns only events in that range.
- [x] `GET /analytics/urls/:shortId/clicks` returns `{ total, today }`.
- [x] `GET /analytics/summary` with a non-admin token returns 403.
- [x] `GET /analytics/summary` with an admin token returns `{ totalUsers, totalUrls, totalClicks }`.
- [x] `GET /analytics/top-urls?period=7d&limit=5` returns at most 5 results ordered by
      click count descending.
- [x] Publishing an event with an unknown type logs a warning and does not persist it.
- [x] Publishing a `url_clicked` event missing the `shortId` field logs a warning and
      does not persist it.
- [x] Events older than `EVENT_RETENTION_DAYS` are removed by the cleanup job.

## Out of Scope

- Real-time streaming or WebSocket-based analytics
- Per-country or per-device breakdown (belongs in a future geo-analytics spec)
- Migrating or back-filling existing event data
- A dashboard UI (belongs in web-ui spec)

## Tasks

<!-- Generated by Claude from the requirements above. Do not write these manually. -->

- [x] **Task 1 — Schema: add indexes to Event table (R9)**
      Add Prisma migration adding indexes on `type`, `createdAt`, and the composite
      `(type, createdAt)` to `analytics-service/prisma/schema.prisma`.

- [x] **Task 2 — Auth: require JWT on GET /events; scope by role (R1)**
      Wire the shared `authenticateJWT` + `isAdmin` middleware. Admin callers receive all
      events; regular users receive only events whose `payload.userId` matches the token
      subject. Return 401 for missing/invalid tokens.

- [x] **Task 3 — GET /events: pagination (R2)**
      Accept `page` (default 1) and `limit` (default 50) query params. Return
      `{ data, pagination: { page, limit, total } }`. Use `skip`/`take` + `count` — no
      in-memory loading.

- [x] **Task 4 — GET /events: filtering (R3)**
      Accept `type`, `userId`, `from` (ISO date), and `to` (ISO date) query params and
      translate them into Prisma `where` conditions. Combine with the auth scope from Task 2.

- [x] **Task 5 — Event payload validation (R8)**
      Define per-type JSON schemas (one per event type the service consumes). Validate each
      incoming RabbitMQ payload before persisting; log a warning and discard on failure.

- [x] **Task 6 — Endpoint: GET /analytics/urls/:shortId/clicks (R4)**
      Query the Event table for `url_clicked` events matching `shortId`. Return
      `{ total, today }` using database-level `COUNT` — no row hydration.

- [x] **Task 7 — Endpoint: GET /analytics/summary (admin, R5)**
      Admin-only endpoint. Return `{ totalUsers, totalUrls, totalClicks }` using three
      separate `COUNT` queries against the Event table by type.

- [x] **Task 8 — Endpoint: GET /analytics/top-urls (admin, R6)**
      Admin-only endpoint. Accept `period` (`7d`, `30d`, etc.) and `limit` (default 10)
      query params. Use `GROUP BY` + `COUNT` at the database level; return results ordered
      by click count descending.

- [x] **Task 9 — Daily cleanup job (R7)**
      On service startup, schedule a job (using `node-cron` or `setInterval`) that runs
      once per day and deletes events older than `EVENT_RETENTION_DAYS` (default 90).

- [x] **Task 10 — Tests**
      Write integration/unit tests covering all acceptance criteria: auth scoping, pagination,
      filtering, new endpoints, payload validation rejection, and cleanup job execution.
