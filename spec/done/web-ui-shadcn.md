# web-ui: Migrate to shadcn/ui component library

> **Status:** done
> **Service(s):** web-ui
> **Priority:** medium

## Goal

Replace the hand-rolled Tailwind class strings scattered across every page with
shadcn/ui components, making the UI consistent, accessible, and easier to extend
without touching business logic.

## Background

The web-ui uses Tailwind CSS but has no component abstractions. Every page
repeats the same long className strings for buttons and inputs, builds its own
alert boxes, and implements accessibility-sensitive widgets (tabs, dialogs) from
scratch without ARIA support. Errors are silently swallowed with `console.error`
and never shown to the user.

shadcn/ui is the standard component layer for Next.js + Tailwind + TypeScript
projects. It copies component source files into the project (no runtime
dependency), uses Radix UI for interactive primitives, and works with the
existing Tailwind setup without adding a second styling system.

## Requirements

### Functional

- **R1:** The project shall be initialised with shadcn/ui (`npx shadcn@latest init`)
  and a `components.json` configuration file committed to the repository.
- **R2:** The following shadcn/ui components shall be installed and used:
  `Button`, `Input`, `Label`, `Card`, `Tabs`, `Badge`, `Alert`, `AlertDialog`,
  `Table`, `Toaster` (sonner).
- **R3:** All pages shall use `Button` instead of raw `<button>` elements with
  inline Tailwind class strings.
- **R4:** All form fields shall use `Input` and `Label` instead of raw `<input>`
  and `<label>` elements with inline Tailwind class strings.
- **R5:** All page card containers (white rounded boxes) shall use `Card`,
  `CardHeader`, and `CardContent`.
- **R6:** The profile page tabs shall use the shadcn/ui `Tabs` component
  (backed by Radix) instead of the hand-rolled tab buttons.
- **R7:** Success and error messages on the profile page shall use the `Alert`
  component instead of custom-styled divs.
- **R8:** The delete action on the manage page shall be confirmed via an
  `AlertDialog` before the request is sent. Currently deletion is immediate
  with no confirmation.
- **R9:** API errors that are currently silently logged with `console.error`
  on the home, login, register, and manage pages shall be surfaced to the user
  via a toast notification using `Toaster` (sonner).
- **R10:** URL tags on the manage page shall use `Badge` instead of custom
  inline-styled spans.
- **R11:** The URL table on the manage page shall use the shadcn/ui `Table`
  component (`Table`, `TableHeader`, `TableRow`, `TableHead`, `TableBody`,
  `TableCell`).

### Non-Functional

- **R12:** No business logic, API calls, routing, or state management shall
  change as part of this migration — only the rendering layer.
- **R13:** The existing test suite shall remain green after the migration.
- **R14:** The Docker image build for web-ui shall continue to succeed.

## Acceptance Criteria

- [ ] `components.json` exists at `web-ui/components.json` and `npx shadcn@latest add` works without re-running init.
- [ ] `src/components/ui/` contains all installed shadcn component files.
- [ ] No raw `<button className="bg-blue-...">` or `<input className="shadow appearance-none...">` patterns remain in any page.
- [ ] The profile page tabs are keyboard-navigable and have correct ARIA roles.
- [ ] Clicking Delete on the manage page opens a confirmation dialog; the URL is only deleted after confirmation.
- [ ] Submitting the login form with wrong credentials shows a toast error instead of a silent console.error.
- [ ] Submitting the register form with a duplicate email shows a toast error.
- [ ] Shortening a URL that returns an API error shows a toast error on the home page.
- [ ] All existing tests pass (`cd web-ui && npm test`).

## Out of Scope

- Changing any API service or backend code.
- Adding new pages or features beyond what exists today.
- Dark mode support (Tailwind dark mode variables are scaffolded by shadcn init but no dark mode switching UI is added).
- Replacing the NavBar with a shadcn navigation component — the navbar is simple enough to stay as-is.
- Adding form validation libraries (e.g. react-hook-form, zod) — validation logic already exists in the pages.

## Docs to Update

- [ ] `web-ui/CLAUDE.md` — update project structure and note shadcn/ui usage.

## Tasks

<!-- Generated by Claude from the requirements above. Do not write these manually. -->

### Task 1 — Initialise shadcn/ui

Run `npx shadcn@latest init` inside `web-ui/` with the default New York style, neutral base colour, and CSS variables enabled. This produces:

- `web-ui/components.json` — shadcn configuration
- `web-ui/src/lib/utils.ts` — `cn()` helper (tailwind-merge + clsx)
- Updated `tailwind.config.ts` and `globals.css` with CSS variable design tokens

Commit all generated/modified files. Do not install component files yet (that is Task 2).

Acceptance: `web-ui/components.json` exists and `npx shadcn@latest add button --dry-run` exits 0.

### Task 2 — Install all required shadcn/ui component files

From `web-ui/`, run:

```
npx shadcn@latest add button input label card tabs badge alert alert-dialog table sonner
```

This copies source files into `web-ui/src/components/ui/`. Also installs the required npm peer packages (`@radix-ui/*`, `sonner`, `lucide-react`, `class-variance-authority`, `clsx`, `tailwind-merge`).

Commit all generated component files and the updated `package.json`/`package-lock.json`.

Acceptance: `web-ui/src/components/ui/` contains `button.tsx`, `input.tsx`, `label.tsx`, `card.tsx`, `tabs.tsx`, `badge.tsx`, `alert.tsx`, `alert-dialog.tsx`, `table.tsx`, and `sonner.tsx`.

### Task 3 — Add Toaster to the root layout

In `web-ui/src/app/layout.tsx`:

- Import `Toaster` from `@/components/ui/sonner`
- Add `<Toaster />` inside `<body>` after `{children}`

No other changes to the layout.

### Task 4 — Migrate login page

File: `web-ui/src/app/login/page.tsx`

Replace raw elements with shadcn/ui components:

- Wrap the form in a `Card` / `CardHeader` / `CardContent`
- Replace `<label>` with `Label`
- Replace `<input>` with `Input`
- Replace `<button type="submit">` and the Register navigation button with `Button`
- In `handleSubmit`, replace `console.error` with `toast.error(...)` from `sonner`
  - Extract a user-friendly message from the Axios error response body or fall back to `'Login failed'`

### Task 5 — Migrate register page

File: `web-ui/src/app/register/page.tsx`

Same pattern as Task 4:

- Wrap the form in `Card` / `CardHeader` / `CardContent`
- Replace `<label>` / `<input>` with `Label` / `Input`
- Replace both `<button>` elements with `Button`
- In `handleSubmit`, replace `console.error` with `toast.error(...)` using the error response message or `'Registration failed'`

### Task 6 — Migrate home page

File: `web-ui/src/app/page.tsx`

- Wrap the form in `Card` / `CardContent`
- Replace `<label>` with `Label`, `<input>` with `Input`
- Replace both `<button>` elements with `Button`
- In `handleSubmit`, replace `console.error` with `toast.error(...)` using the error response message or `'Failed to shorten URL'`

### Task 7 — Migrate manage page

File: `web-ui/src/app/manage/page.tsx`

- Replace the raw `<table>` and its children with shadcn `Table`, `TableHeader`, `TableRow`, `TableHead`, `TableBody`, `TableCell`
- Replace the inline `<input>` in the edit cell with `Input`
- Replace the Save / Edit / Delete `<button>` elements with `Button` (use `variant="outline"` for Edit, `variant="destructive"` for Delete, `variant="default"` for Save)
- Replace the custom-styled tag `<span>` elements (alias, expiry, tags) with `Badge` using appropriate variants (`secondary` for alias, `outline` for expiry, `default` for user tags)
- Wrap the whole content area in a `Card` / `CardContent`
- Add an `AlertDialog` confirmation before the delete request:
  - Clicking Delete opens the dialog instead of calling `handleDelete` directly
  - The dialog shows the short ID of the URL to be deleted
  - Confirming calls `handleDelete`; cancelling does nothing
- Replace all `console.error` calls with `toast.error(...)` from `sonner`

### Task 8 — Migrate profile page

File: `web-ui/src/app/profile/page.tsx`

- Wrap the whole page card in `Card` / `CardHeader` / `CardContent`
- Replace the hand-rolled tab buttons with the shadcn `Tabs` / `TabsList` / `TabsTrigger` / `TabsContent` components backed by Radix UI
  - Remove the `activeTab` state; let Radix manage tab state via `defaultValue="profile"`
- Replace the success/error message `<div>` elements with shadcn `Alert` + `AlertDescription`
  - Use `variant="destructive"` for errors and the default variant for success
- Replace all `<label>` with `Label`, all `<input>` with `Input`
- Replace all `<button>` elements with `Button`

### Task 9 — Update web-ui/CLAUDE.md

Create `web-ui/CLAUDE.md` documenting:

- Project structure (pages, components, services, lib)
- That shadcn/ui is used as the component library; component files live in `src/components/ui/`
- How to add new shadcn components: `npx shadcn@latest add <component>`
- Test setup (vitest + testing-library)

### Task 10 — Verify tests pass

Run `cd web-ui && npm test` and confirm all existing tests remain green. No test changes are expected; this task is a verification gate.
