apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: bearlink
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # Headless — required for StatefulSet DNS (postgres-0.postgres)
