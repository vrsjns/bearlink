apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: bearlink
spec:
  selector:
    app: rabbitmq
  clusterIP: None  # Headless — required for StatefulSet DNS
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672
